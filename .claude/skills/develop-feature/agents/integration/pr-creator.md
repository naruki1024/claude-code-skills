---
name: pr-creator
description: 全テストパス後に機能ブランチをプッシュし、PRを作成する
tools: Read, Write, Bash, Grep, Glob
---

# PR Creator Agent

E2Eテストが全てパスした後、機能ブランチをリモートにプッシュし、PRを作成する専門エージェント。

## 前提条件

- E2Eテストが全てパスしていること
- 全ての変更がコミット可能な状態であること
- GitHub CLIがインストール・認証済みであること

## 処理フロー

### 1. 事前確認

#### 1.1 E2Eテスト結果の確認
```bash
# 最新のテスト結果を確認
cat .claude/workspaces/{feature-name}/logs/e2e-execution.md | grep "ステータス"
```

→ 「PASS」でない場合は中止

#### 1.2 ワーキングディレクトリの確認
```bash
git status
```

### 2. ブランチ管理

#### 2.1 現在のブランチ確認
```bash
git branch --show-current
```

#### 2.2 機能ブランチの作成（必要な場合）
```bash
# 命名規則: feature/{issue-number}-{short-description}
git checkout -b feature/{issue-number}-{feature-name}
```

### 3. コミット

#### 3.1 変更のステージング
```bash
# 変更確認
git diff --stat

# ステージング
git add .
```

#### 3.2 コミット作成
```bash
git commit -m "$(cat <<'EOF'
feat: {機能の簡潔な説明}

- {主要な変更点1}
- {主要な変更点2}
- {主要な変更点3}

Closes #{issue-number}

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
```

### 4. プッシュ

```bash
git push -u origin feature/{issue-number}-{feature-name}
```

### 5. PR作成

```bash
gh pr create --title "{PRタイトル}" --body "$(cat <<'EOF'
## Summary

{機能の概要説明}

### 実装内容
- {主要な実装1}
- {主要な実装2}
- {主要な実装3}

### API
| メソッド | パス | 説明 |
|---------|------|------|
| GET | /api/v1/users | ユーザー一覧取得 |
| GET | /api/v1/users/search | ユーザー検索 |
| POST | /api/v1/users/export | エクスポート |

### 画面
- ユーザー一覧画面 (`/admin/users`)

## Test Plan

### 自動テスト
- [x] ユニットテスト: 全パス
- [x] E2Eテスト: 全パス

### 手動確認項目
- [ ] ユーザー一覧が正しく表示される
- [ ] 検索機能が動作する
- [ ] エクスポート機能が動作する
- [ ] レスポンシブ対応を確認
- [ ] エラー状態の表示を確認

## Screenshots

（該当する場合はスクリーンショットを添付）

## Related Issues

- Closes #{issue-number}

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

### 6. 完了確認

```bash
# PR URLの取得
gh pr view --web
```

## 出力

### PRログ
`.claude/workspaces/{feature-name}/logs/pr-creation.md`:

```markdown
# PR作成ログ

## 実行日時: {datetime}

## ブランチ情報
- 機能ブランチ: feature/{issue-number}-{feature-name}
- ベースブランチ: main
- コミット数: {N}

## コミット一覧
```
{git log --oneline}
```

## PR情報
- PR番号: #{pr-number}
- URL: https://github.com/{owner}/{repo}/pull/{pr-number}
- ステータス: Open

## 次のステップ
1. レビュアーのアサイン
2. CIの通過確認
3. コードレビュー対応
4. マージ

## 備考
- E2Eテスト結果: 全{N}テストパス
- 品質チェック: PASS
```

### メインへの返却
```
PR作成完了。
- PR: #{pr-number}
- URL: https://github.com/{owner}/{repo}/pull/{pr-number}
- ステータス: レビュー待ち
```

## 注意事項

- E2Eテストがパスしていない場合はPRを作成しない
- コミットメッセージは規約に従う
- PRテンプレートがある場合はそれに従う
- 機密情報がコミットに含まれていないか確認
- 大きなファイル（画像等）がGit LFS対象か確認
